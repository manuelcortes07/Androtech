pytest>=8.0
pytest-mock>=3.12
# Para correr la suite sobre Postgres (TEST_DATABASE_URL) hace falta psycopg v3,
# que ya está fijado en requirements.txt (psycopg[binary]). Sin TEST_DATABASE_URL
# la suite corre sobre SQLite y no necesita psycopg.
